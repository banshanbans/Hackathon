# coding: utf-8

"""
    SoloShot AI API

    Contract-first API for SoloShot AI. All normalized coordinates use the [0,1] range with a top-left origin; x grows right and y grows down. W3 adds a short-lived, capability-secured H5 to iOS handoff surface.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CurrentAlignment(BaseModel):
    """
    CurrentAlignment
    """ # noqa: E501
    schema_version: StrictStr
    person_detected: StrictBool
    multiple_people: StrictBool
    full_body_visible: StrictBool
    position_status: StrictStr
    scale_status: StrictStr
    pose_status: StrictStr
    instruction_code: StrictStr
    ready_to_capture: StrictBool
    stability_score: Union[Annotated[float, Field(le=1, strict=True, ge=0)], Annotated[int, Field(le=1, strict=True, ge=0)]]
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["schema_version", "person_detected", "multiple_people", "full_body_visible", "position_status", "scale_status", "pose_status", "instruction_code", "ready_to_capture", "stability_score"]

    @field_validator('schema_version')
    def schema_version_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['1.0']):
            raise ValueError("must be one of enum values ('1.0')")
        return value

    @field_validator('position_status')
    def position_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['move_left', 'move_right', 'centered', 'unknown']):
            raise ValueError("must be one of enum values ('move_left', 'move_right', 'centered', 'unknown')")
        return value

    @field_validator('scale_status')
    def scale_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['move_forward', 'move_backward', 'correct', 'unknown']):
            raise ValueError("must be one of enum values ('move_forward', 'move_backward', 'correct', 'unknown')")
        return value

    @field_validator('pose_status')
    def pose_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['acceptable', 'adjust', 'unknown']):
            raise ValueError("must be one of enum values ('acceptable', 'adjust', 'unknown')")
        return value

    @field_validator('instruction_code')
    def instruction_code_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['no_person', 'multiple_people', 'move_left', 'move_right', 'move_forward', 'move_backward', 'feet_outside', 'head_outside', 'adjust_body_direction', 'adjust_arm', 'hold_still', 'ready_to_capture']):
            raise ValueError("must be one of enum values ('no_person', 'multiple_people', 'move_left', 'move_right', 'move_forward', 'move_backward', 'feet_outside', 'head_outside', 'adjust_body_direction', 'adjust_arm', 'hold_still', 'ready_to_capture')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CurrentAlignment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CurrentAlignment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "schema_version": obj.get("schema_version"),
            "person_detected": obj.get("person_detected"),
            "multiple_people": obj.get("multiple_people"),
            "full_body_visible": obj.get("full_body_visible"),
            "position_status": obj.get("position_status"),
            "scale_status": obj.get("scale_status"),
            "pose_status": obj.get("pose_status"),
            "instruction_code": obj.get("instruction_code"),
            "ready_to_capture": obj.get("ready_to_capture"),
            "stability_score": obj.get("stability_score")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


