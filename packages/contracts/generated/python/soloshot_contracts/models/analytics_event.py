# coding: utf-8

"""
    SoloShot AI API

    Contract-first API for SoloShot AI. All normalized coordinates use the [0,1] range with a top-left origin; x grows right and y grows down. W3 adds a short-lived, capability-secured H5 to iOS handoff surface.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AnalyticsEvent(BaseModel):
    """
    AnalyticsEvent
    """ # noqa: E501
    schema_version: StrictStr
    event_id: UUID
    event_name: StrictStr
    session_id: Annotated[str, Field(strict=True)]
    source_channel: StrictStr
    client: StrictStr
    timestamp: datetime
    properties: Dict[str, Any] = Field(description="Non-sensitive event attributes only. Media bytes, tokens, and signed URLs are forbidden by policy.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["schema_version", "event_id", "event_name", "session_id", "source_channel", "client", "timestamp", "properties"]

    @field_validator('schema_version')
    def schema_version_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['1.0']):
            raise ValueError("must be one of enum values ('1.0')")
        return value

    @field_validator('event_name')
    def event_name_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['page_view', 'reference_select', 'reference_upload', 'circle_complete', 'replicate_click', 'mode_select', 'agent_start', 'agent_success', 'agent_fail', 'shot_plan_view', 'h5_capture_start', 'result_upload', 'result_evaluated', 'handoff_qr_create', 'handoff_claimed', 'share_click', 'publish_preview']):
            raise ValueError("must be one of enum values ('page_view', 'reference_select', 'reference_upload', 'circle_complete', 'replicate_click', 'mode_select', 'agent_start', 'agent_success', 'agent_fail', 'shot_plan_view', 'h5_capture_start', 'result_upload', 'result_evaluated', 'handoff_qr_create', 'handoff_claimed', 'share_click', 'publish_preview')")
        return value

    @field_validator('session_id')
    def session_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^ss_[A-Za-z0-9_-]+$", value):
            raise ValueError(r"must validate the regular expression /^ss_[A-Za-z0-9_-]+$/")
        return value

    @field_validator('source_channel')
    def source_channel_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['h5_qr', 'h5_direct', 'ios', 'demo_preset', 'shared_link']):
            raise ValueError("must be one of enum values ('h5_qr', 'h5_direct', 'ios', 'demo_preset', 'shared_link')")
        return value

    @field_validator('client')
    def client_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['h5', 'ios', 'api']):
            raise ValueError("must be one of enum values ('h5', 'ios', 'api')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AnalyticsEvent from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AnalyticsEvent from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "schema_version": obj.get("schema_version"),
            "event_id": obj.get("event_id"),
            "event_name": obj.get("event_name"),
            "session_id": obj.get("session_id"),
            "source_channel": obj.get("source_channel"),
            "client": obj.get("client"),
            "timestamp": obj.get("timestamp"),
            "properties": obj.get("properties")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


