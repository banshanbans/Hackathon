//
// ApiError.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ApiError: Sendable, Codable, Hashable {

    public enum SchemaVersion: String, Sendable, Codable, CaseIterable {
        case _10 = "1.0"
    }
    public enum Code: String, Sendable, Codable, CaseIterable {
        case referenceNoPerson = "REFERENCE_NO_PERSON"
        case referenceMultiplePeople = "REFERENCE_MULTIPLE_PEOPLE"
        case referenceParseFailed = "REFERENCE_PARSE_FAILED"
        case modelTimeout = "MODEL_TIMEOUT"
        case invalidJson = "INVALID_JSON"
        case lowConfidence = "LOW_CONFIDENCE"
        case captureUploadFailed = "CAPTURE_UPLOAD_FAILED"
        case compareFailed = "COMPARE_FAILED"
        case videoRenderFailed = "VIDEO_RENDER_FAILED"
        case handoffExpired = "HANDOFF_EXPIRED"
        case handoffAlreadyClaimed = "HANDOFF_ALREADY_CLAIMED"
        case sessionExpired = "SESSION_EXPIRED"
        case unsupportedMedia = "UNSUPPORTED_MEDIA"
        case schemaVersionUnsupported = "SCHEMA_VERSION_UNSUPPORTED"
        case validationFailed = "VALIDATION_FAILED"
        case notFound = "NOT_FOUND"
        case internalError = "INTERNAL_ERROR"
    }
    public static let requestIdRule = StringRule(minLength: 1, maxLength: 128, pattern: nil)
    public static let messageRule = StringRule(minLength: 1, maxLength: 500, pattern: nil)
    public static let retryAfterRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var schemaVersion: SchemaVersion
    public var requestId: String
    public var code: Code
    public var message: String
    public var recoverable: Bool
    public var retryAfter: Int?

    public init(schemaVersion: SchemaVersion, requestId: String, code: Code, message: String, recoverable: Bool, retryAfter: Int? = nil) {
        self.schemaVersion = schemaVersion
        self.requestId = requestId
        self.code = code
        self.message = message
        self.recoverable = recoverable
        self.retryAfter = retryAfter
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case schemaVersion = "schema_version"
        case requestId = "request_id"
        case code
        case message
        case recoverable
        case retryAfter = "retry_after"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(schemaVersion, forKey: .schemaVersion)
        try container.encode(requestId, forKey: .requestId)
        try container.encode(code, forKey: .code)
        try container.encode(message, forKey: .message)
        try container.encode(recoverable, forKey: .recoverable)
        try container.encodeIfPresent(retryAfter, forKey: .retryAfter)
    }
}

