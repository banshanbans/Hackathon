//
// CurrentAlignment.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CurrentAlignment: Sendable, Codable, Hashable {

    public enum SchemaVersion: String, Sendable, Codable, CaseIterable {
        case _10 = "1.0"
    }
    public enum PositionStatus: String, Sendable, Codable, CaseIterable {
        case moveLeft = "move_left"
        case moveRight = "move_right"
        case centered = "centered"
        case unknown = "unknown"
    }
    public enum ScaleStatus: String, Sendable, Codable, CaseIterable {
        case moveForward = "move_forward"
        case moveBackward = "move_backward"
        case correct = "correct"
        case unknown = "unknown"
    }
    public enum PoseStatus: String, Sendable, Codable, CaseIterable {
        case acceptable = "acceptable"
        case adjust = "adjust"
        case unknown = "unknown"
    }
    public enum InstructionCode: String, Sendable, Codable, CaseIterable {
        case noPerson = "no_person"
        case multiplePeople = "multiple_people"
        case moveLeft = "move_left"
        case moveRight = "move_right"
        case moveForward = "move_forward"
        case moveBackward = "move_backward"
        case feetOutside = "feet_outside"
        case headOutside = "head_outside"
        case adjustBodyDirection = "adjust_body_direction"
        case adjustArm = "adjust_arm"
        case holdStill = "hold_still"
        case readyToCapture = "ready_to_capture"
    }
    public static let stabilityScoreRule = NumericRule<Double>(minimum: 0, exclusiveMinimum: false, maximum: 1, exclusiveMaximum: false, multipleOf: nil)
    public var schemaVersion: SchemaVersion
    public var personDetected: Bool
    public var multiplePeople: Bool
    public var fullBodyVisible: Bool
    public var positionStatus: PositionStatus
    public var scaleStatus: ScaleStatus
    public var poseStatus: PoseStatus
    public var instructionCode: InstructionCode
    public var readyToCapture: Bool
    public var stabilityScore: Double

    public init(schemaVersion: SchemaVersion, personDetected: Bool, multiplePeople: Bool, fullBodyVisible: Bool, positionStatus: PositionStatus, scaleStatus: ScaleStatus, poseStatus: PoseStatus, instructionCode: InstructionCode, readyToCapture: Bool, stabilityScore: Double) {
        self.schemaVersion = schemaVersion
        self.personDetected = personDetected
        self.multiplePeople = multiplePeople
        self.fullBodyVisible = fullBodyVisible
        self.positionStatus = positionStatus
        self.scaleStatus = scaleStatus
        self.poseStatus = poseStatus
        self.instructionCode = instructionCode
        self.readyToCapture = readyToCapture
        self.stabilityScore = stabilityScore
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case schemaVersion = "schema_version"
        case personDetected = "person_detected"
        case multiplePeople = "multiple_people"
        case fullBodyVisible = "full_body_visible"
        case positionStatus = "position_status"
        case scaleStatus = "scale_status"
        case poseStatus = "pose_status"
        case instructionCode = "instruction_code"
        case readyToCapture = "ready_to_capture"
        case stabilityScore = "stability_score"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(schemaVersion, forKey: .schemaVersion)
        try container.encode(personDetected, forKey: .personDetected)
        try container.encode(multiplePeople, forKey: .multiplePeople)
        try container.encode(fullBodyVisible, forKey: .fullBodyVisible)
        try container.encode(positionStatus, forKey: .positionStatus)
        try container.encode(scaleStatus, forKey: .scaleStatus)
        try container.encode(poseStatus, forKey: .poseStatus)
        try container.encode(instructionCode, forKey: .instructionCode)
        try container.encode(readyToCapture, forKey: .readyToCapture)
        try container.encode(stabilityScore, forKey: .stabilityScore)
    }
}

