//
// AnalyticsEvent.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AnalyticsEvent: Sendable, Codable, Hashable {

    public enum SchemaVersion: String, Sendable, Codable, CaseIterable {
        case _10 = "1.0"
    }
    public enum EventName: String, Sendable, Codable, CaseIterable {
        case pageView = "page_view"
        case referenceSelect = "reference_select"
        case referenceUpload = "reference_upload"
        case circleComplete = "circle_complete"
        case replicateClick = "replicate_click"
        case modeSelect = "mode_select"
        case agentStart = "agent_start"
        case agentSuccess = "agent_success"
        case agentFail = "agent_fail"
        case shotPlanView = "shot_plan_view"
        case h5CaptureStart = "h5_capture_start"
        case resultUpload = "result_upload"
        case resultEvaluated = "result_evaluated"
        case handoffQrCreate = "handoff_qr_create"
        case handoffClaimed = "handoff_claimed"
        case shareClick = "share_click"
        case publishPreview = "publish_preview"
    }
    public enum SourceChannel: String, Sendable, Codable, CaseIterable {
        case h5Qr = "h5_qr"
        case h5Direct = "h5_direct"
        case ios = "ios"
        case demoPreset = "demo_preset"
        case sharedLink = "shared_link"
    }
    public enum Client: String, Sendable, Codable, CaseIterable {
        case h5 = "h5"
        case ios = "ios"
        case api = "api"
    }
    public static let sessionIdRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^ss_[A-Za-z0-9_-]+$/")
    public var schemaVersion: SchemaVersion
    public var eventId: UUID
    public var eventName: EventName
    public var sessionId: String
    public var sourceChannel: SourceChannel
    public var client: Client
    public var timestamp: Date
    /** Non-sensitive event attributes only. Media bytes, tokens, and signed URLs are forbidden by policy. */
    public var properties: [String: JSONValue]

    public init(schemaVersion: SchemaVersion, eventId: UUID, eventName: EventName, sessionId: String, sourceChannel: SourceChannel, client: Client, timestamp: Date, properties: [String: JSONValue]) {
        self.schemaVersion = schemaVersion
        self.eventId = eventId
        self.eventName = eventName
        self.sessionId = sessionId
        self.sourceChannel = sourceChannel
        self.client = client
        self.timestamp = timestamp
        self.properties = properties
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case schemaVersion = "schema_version"
        case eventId = "event_id"
        case eventName = "event_name"
        case sessionId = "session_id"
        case sourceChannel = "source_channel"
        case client
        case timestamp
        case properties
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(schemaVersion, forKey: .schemaVersion)
        try container.encode(eventId, forKey: .eventId)
        try container.encode(eventName, forKey: .eventName)
        try container.encode(sessionId, forKey: .sessionId)
        try container.encode(sourceChannel, forKey: .sourceChannel)
        try container.encode(client, forKey: .client)
        try container.encode(timestamp, forKey: .timestamp)
        try container.encode(properties, forKey: .properties)
    }
}

